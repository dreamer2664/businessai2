# Diagnosis — the "best deals list" session (owner's transcript, 2026-09-10 02:40)

What the owner saw, what actually happened in the code, and the fix for each. Written before touching anything.

| # | What the owner saw | Root cause (file) | Fix |
|---|---|---|---|
| 1 | "Take around 5-6 hours" → plan said **quick** | `_QUICK` in brief.py matches the word *subito* — Italian for "right away" — so the site name **subito.it** set the pace to quick. | Pace words never match a site name (`subito.it`, "on subito", "su subito", "subito, vinted"). Only "subito" as an adverb ("fallo subito", "subito!") counts. |
| 2 | "Take around 5-6 hours" was not read as a time budget | `_FLOOR` needs "at least / minimum / take" followed directly by a number; "take **around** 5-6 hours" and ranges "5-6" are not parsed. | `_FLOOR` accepts *around/about/roughly/circa/~* and ranges *5-6 / 5 to 6 / 5–6* (the lower bound is the floor, the upper the budget). |
| 3 | Owner: *"I said take 5-6 hours and you put quick… change that"* → bot: **"Speeding up"** | `HURRY` in mind.py matches the word *quick* anywhere, even quoted, even in a complaint that says "slow". Also: NO_RUSH is checked *after* HURRY and doesn't know "slow down". | Interrupt logic: a message containing *slow / slower / slow down / take your time / 5 hours* wins over HURRY; quoted words ("quick") don't count; a duration in the message becomes the new floor/budget mid-job (`pace.slow_now(mins)`). |
| 4 | *"No, I said slow down!!"* and *"Slow down, take it slowly"* were **queued as new jobs** and later ran as `summarize slow down` → browser tried to open `https://slow down` | (a) `mind.interrupt` treats them as new requests; (b) `_rule_brief` turns any short imperative into a job; (c) `tasks.summarize` accepts a non-URL topic and prepends https://. | (a) they are pace changes, answered live ("Slowing down — …"); (b) a message with no topic and only pace words → `kind=chat`; (c) `summarize` without a URL answers in words instead of navigating. |
| 5 | Three "stop" messages → same "Stopping …" line three times; job went on 50 s | stop sets a flag; the seller check only checks `should_stop()` between listings and continues writing the document. Repeat "stop" is not recognised. | Second "stop" within 60 s → "Still stopping — the current page finishes in a moment, then I hand over." and the browser page is closed to break the wait. |
| 6 | The topic became **"facebook marketplace is only ok if they're in the city of barletta"** and the plan showed no list | `topic_of` picks the *longest* comma/period chunk after stripping; the owner's real object is "this list that I'm about to send" — a list that doesn't exist yet. | Brief detects "the list I'm about to send / I'll send you / attached" → the plan **waits for the list** (asks: "Send the list — one item per line — and I start"). The BARLETTA sentence is kept as a *condition*, never as the topic. |
| 7 | It ended up on a **Baricco novel** on Vinted | With topic = that sentence, `markets.search_market(vinted, "facebook marketplace is only ok if…")` returned whatever Vinted matched on "city"… The search was for the wrong words. Also `seller_check` was chosen instead of `research` (rule: "find … deals" → seller_check). | Consequence of #6. Plus: a market search whose query is > 8 words or contains condition words (only/if/must/in the city of) is refused with a plain note instead of run. |
| 8 | Cookie banners: "it just stays still" | `Browser.open()` never calls `dismiss_banner()`; only some callers do (markets, sellers, shopfacts) — `tasks.visit`, `summarize`, `research` pages don't. Live test: Vinted's banner is inside a consent iframe that loads *after* networkidle; Subito's Didomi popup is caught (but "Accetta" was clicked instead of "Continua senza accettare" because the arrow "→" broke the regex). | `open()` dismisses banners itself after load and again after 1.5 s (late iframes); regex tolerates trailing arrows/punctuation; reject-first fixed for Didomi; per-site cookie memory (accepted once → the context keeps the cookie). |
| 9 | "What time is it in the UK" → *"I don't know the time zone of 'the UK'"* | `clock()` looks up the key "the uk"; the article isn't stripped, and "united kingdom" is missing from the table. | Strip articles; add united kingdom / great britain / britain / scotland / wales / ireland / usa / united states / europe. |
| 10 | Plan said *"Your conditions: I want you to find me the best deals…"* | The "i want …" condition rule grabs the whole request. | "I want you to <verb>" is the request itself, not a condition. |
| 11 | "From last time: … could not open https://i said slow down" advice | Consequence of #4 (a machine-looking failure that is really a bad plan). | Fixed by #4; the mind also treats `ERR_NAME_NOT_RESOLVED` on a non-URL as a planning fault, not advice. |

## Sites the owner wants trained now
vinted, subito, wallapop, temu, shein, dhgate, banggood — each gets: cookie banner handled, a search URL, a result-card reader, a listing reader, and a live score check (skipped gracefully when the sandbox is walled).
