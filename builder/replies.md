# Builder → owner (newest at the bottom)

- 2026-09-08 19:40 (builder): New channel is live. From PowerShell:
  `wsl bash -lc "cd ~/businessai && git pull && python3 scripts/say.py 'your message'"` → leaves me a note.
  `wsl bash -lc "cd ~/businessai && python3 scripts/say.py"` → prints my replies (add `--watch` to keep listening).
  A one-word "go" in the chat wakes me; I read owner/inbox.md first, answer here.
- 2026-09-08 19:40 (builder): About the Telegram log you sent — 5 bugs found, fixes in progress:
  1) your request no longer queues behind self-training (self-training is filler and gets cut at once);
  2) "stop"/"wait"/"basta" act immediately, never go to the brain (no more circuit-breaker lectures);
  3) "stop" also stops self-training and reports what was running;
  4) the plan repeats your sites (Vinted, subito.it) and conditions ("shipping on, not hand-offs") back to you;
  5) a one-word message asks for more words instead of guessing; long requests get "👀 On it" within a second.
- 2026-09-08 19:55 (builder): Fixes from your Telegram log are pushed (e04e4aa). Update the PC: `cd ~/businessai && git pull && sh scripts/service.sh`.
  Then re-try the same thing: send the Kawhi Leonard request → the plan should name Vinted + subito.it and your "shipping on, not hand-offs" condition, and start at once even if it was studying. "stop" answers in words, immediately.
  New test suite: score_stop 11/11; brief 34/34; talk 460/460.
- 2026-09-08 20:35 (builder, new): The old chat and its token were lost. New home: **github.com/dreamer2664/businessai2** — all 154 commits
  pushed, the `latest` release re-created with the same five assets (brain.kdr, business.kdw, operations.kdw, kdr-brain-lite,
  llama-server-static); every script/URL now points at businessai2. On the PC: `cd ~ && git clone https://github.com/dreamer2664/businessai2`
  (or in the existing folder: `git remote set-url origin https://github.com/dreamer2664/businessai2.git && git pull`), copy your old
  `.secrets/env` across and set `GH_REPO=businessai2` + the new token in it.
  Health check in the new sandbox before the move: brief 34/34, stop 11/11, talk 460/460, mind 42/42, store 40/40, phone 23/23, sites 41/41,
  sellers 34/34, docs 20/20, accounts 12/12, study 20/20, rehearsal 19/19, walls 14/14, channels 36/36, inbox 23/23, packs 52/55 + 39/41,
  shopfacts 15/16 (one miss to look at — the model-drafted ones vary a little run to run).
- 2026-09-09 (builder, new): Took over after the old chat was lost again. Verified from scratch: repo HEAD eb67eda, stop 11/11, brief 34/34, mind 42/42, talk 452/460 (8 = Chromium missing in sandbox, env-only), packs 52/55 + 39/41. Telegram: @busyness001bot token valid, nobody polling, 1 stale "connect google" pending. Owner chose: bot home = PC, order = deep read → health check → google. Added scripts/doctor.sh (PowerShell one-liner pulls it via curl, finds/clones repo, pulls, restarts service, prints secrets-safe report).
- 2026-09-09 (builder): PC doctor run #1: bot ALIVE (service active, polling, ate the stale update). 2 fixes: git pull failed (divergent branches — PC HEAD 75a9639 is a local-only commit, unknown to GitHub) → doctor.sh now rebases local commits on top (simulated + verified); GH_OWNER was still the template placeholder (len=20) → doctor now sets dreamer2664. PC note: the google link sent at 00:23 is dead (restart killed the in-memory PKCE verifier) — ignore it, fresh 'connect google' later.
- 2026-09-09 (builder): Mea culpa — doctor v2/v3 never contained the rebase fix: two same-file edits sent in one block raced and the second clobbered the first. PC run #3 proved it (v3 banner + v1 git lines). Re-applied sequentially, read back from disk to confirm. Lesson: never parallel-edit one file. V4 = banner v4 + real rebase block + pull.rebase=true.
- 2026-09-09 (builder): PC doctor run #4 (v4): REBASE OK — 75a9639 turned out to be a content-duplicate of upstream, git dropped the empty replay, PC now exactly on c0c1db4. Secrets all present, GH_OWNER fixed, service active on latest code (e04e4aa fixes live). PC track DONE. Continuing: deep read (talk.py next) + model-backed health checks.
- 2026-09-09 (builder): shopfacts wobble root-caused: 1.5B model mis-copies figures run to run (Spain 4–6 → 4–7; a UK draft answered with Italy/EU times). Old checker only asked "is this span anywhere on the page" — the mis-bound figures passed. Fix on branch fix/country-figures (ef27ecc, main untouched): spans must appear in a fact naming the asked country, else global fallback; synthetic 5/5, Spain passes in full run. BUT the UK catch exposed the next gap: repair defers to the owner ("I will check...") instead of fixing the figure from the facts → test still 15/16 with the miss moving. Next: strengthen repair (fix figures from named facts, don't punt), then full battery (shopfacts/inbox/store) before merging.
- 2026-09-09 (builder): PC bot down at 00:01 UTC (owner's 'connect google' untouched 5+ min — PC asleep or service dead, NOT a code bug: connect path is non-blocking and _load swallows corrupt-json crashes). Standby bot started in sandbox with owner token: pinned owner, answered with fresh OAuth link (catcher :8097 here). Doctor v5: stray-copy killer (409 flakiness), poll-error/crash tail, restart count, client-json validation, uptime.
- 2026-09-09 (builder): Item 9 DONE — the thinking panel. Mind.think_snapshot() (why/status/lessons[3]/queue) + thinking_text() as the single source; Viewer.thinker feeds it into state() and a live #think panel on the owner page (escaped, hidden when idle); /thinking answers the same words in chat; score_think 12/12 in the battery. Wounds: a double-replace ate def stats_text (mind/phone red, restored, 42/42 + 23/23); PAGE display-escaping cost 3 patch attempts (raw anchors from now on). Battery 18/18, 0 FAIL, merged to main. Heartbeat 78%.

- 2026-09-09 (builder): Item 6 DONE — Gmail mastery + safe sign-ups. Gmail: replies land in-thread (threadId + In-Reply-To), answered mail marked read + archived (new gmail.modify scope — free, PC token not minted yet), code/link search qualified server-side with from:. Sign-ups: approval gate (local fakes always fine; new real site asks Allow-once/Always/Never, remembered; NEVER list still hard-refuses), /accounts allow|forget, docs/ACCOUNTS.md, --drill N training rounds on throwaway fakes. Scores: gmail 8/8 (new), fallback 14→17/17, accounts 12→17/17 (gate 5/5 verified here, browser paths on PC). Battery 19/19, 0 FAIL, merged to main. Heartbeat 86%.
- 2026-09-09 (builder): Item 3 DONE — the last one. agent/markets.py: Vinted/subito search+item readers (embedded-JSON first, DOM/text fallback, fixtures in tests/market), Facebook waters-only probe (login→honest stop, never a bypass), Throttle (4s host gap, 30s→10min exponential backoff, give-up), stealth (BAI_STEALTH: UA rotation + webdriver-hide, env-gated) + proxy (BAI_PROXY, env-gated). Wired: Browser launch/context, SellerCheck (direct site search when the brief names one, deep facts merged in read_listing, FB shore note), core passes brief sites. score_markets 17/17 offline here (+3 browser on PC), docs/MARKETS.md. Battery 20/20, 0 FAIL, merged to main. Heartbeat 100% — MARATHON COMPLETE.
- 2026-09-09 (builder): QA sweep after the marathon. Fixed 3 real issues: (1) approval gate treated file:// stage URLs as unknown-real (would have broken operator logins on the PC) — stage-aware now, test locked; (2) Gmail threaded replies could 400 on strict threads and lose the fallback answer — plain-send retry with test (gmail 9/9); (3) inbox missed the rash/allergy escalation 22/23 — one word, now 23/23. Verified: default UA byte-identical, viewer JS node-valid, all non-fast suite gaps env-only (playwright/model/network), talk 451/460 = 8 PNG-render + 1 has-token misses (both env). Docs: added the 5 missing SCORES rows (dropship/think/docs-native/inbox/store), corrected eyes 26→19, accounts 18/18. Polish: Throttle internals, dead _meta removed, host-agnostic card fallbacks, NEVER unapprovable at allow time. Battery 20/20 fast, 0 FAIL, merged to main. Heartbeat 100%.
